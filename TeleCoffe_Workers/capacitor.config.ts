import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'telecoffee.workers',
  appName: 'TeleCoffe_Workers',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
