import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'telecoffee.clients',
  appName: 'TeleCoffee_Clients',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
